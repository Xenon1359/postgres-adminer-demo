# How to run:

```bash
docker compose up --build
```

In browser:

```bash
http://localhost:8080
```

or

``bash``
http://<you_host_ip>:8080
``

# Credentials:

System = db
Server = postgres_db
Username = admin
Password = admin123
Database = testdb
